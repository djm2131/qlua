(include "types.qa0")

(procedure size-of-c-abi-table ([stem "sizeof_cABiTable"]
                                [return total int "size_t"])
           ([l-s    int "Ls"     "size_t"])
   (op int-sub () (x) ([reg l-s] [const 2]))
   (op int-mul () (y) ([reg x] [const (size-of c-abc)]))
   (op int-add () (total) ([reg y] [const (size-of c-AB-Inverse)])))

