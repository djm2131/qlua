(include "types.qa0")

(procedure size-clover ([stem "sizeof_clover"]
                       [return total int "size_t"]
                       prec&color)
           ([volume int "volume" "size_t"])
   (op int-mul () (total) ([reg volume] [const (size-of Clover)])))
